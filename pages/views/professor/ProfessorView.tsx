import * as React from 'react';
import BaseProps from '../BaseProps';

class ProfessorView extends React.Component<BaseProps> {
  public render() {
    return (
      <main>
        <h1>PROFESSSOR NAME</h1>
      </main>
    );
  }
}

export default ProfessorView;
